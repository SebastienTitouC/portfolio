import { m as c, c as l, i as a } from "./dark-mode-rPT3-25Y.js"; const u = () => { const s = document.querySelector(".hero__function_typewriterJS"), t = "Je suis ingénieur en développement logiciel, mais je suis surtout un fan de nouvelles technologies. "; function r(e) { e < t.length ? setTimeout(() => { s.innerHTML = t.slice(0, e), r(e + 1) }, 40) : (setTimeout(() => document.querySelector(".social__button:nth-child(1)").classList.remove("hidden"), 800), setTimeout(() => document.querySelector(".social__button:nth-child(2)").classList.remove("hidden"), 1050), setTimeout(() => document.querySelector(".social__button:nth-child(3)").classList.remove("hidden"), 1200), setTimeout(() => document.querySelector(".social__button:nth-child(4)").classList.remove("hidden"), 1400), setTimeout(() => document.querySelector(".social__button:nth-child(5)").classList.remove("hidden"), 1600)) } setTimeout(() => r(42), 2e3) }, m = () => { function s(e, n) { e.forEach(o => { o.isIntersecting && o.intersectionRatio >= .98 ? o.target.classList.remove("invisible") : o.target.classList.add("invisible") }) } const t = new IntersectionObserver(s, { root: null, rootMargin: "0px", threshold: [.95, 1] }); document.querySelectorAll(".projects__card").forEach(e => { t.observe(e) }) }, d = () => { document.querySelectorAll(".projects__skill").forEach((s, t) => { s.style.animationDelay = `${t * 2}s` }), document.querySelectorAll(".projects__skill2").forEach((s, t) => { s.style.animationDelay = `${t * 2 - 2}s` }) }, h = async () => { const s = document.querySelector(".contact__contact-form"); function t(e) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(e).toLowerCase()) } const r = async e => { if (e.preventDefault(), console.log("Formulaire soumis"), document.querySelector(".contact__honeypot input").value != "") { e.target.closest("form").reset(); return } let n = { n: e.target[0].value, e: e.target[1].value, m: e.target[3].value, h: "" }; if (t(n.e)) { console.log("Email valide "); try { const o = await fetch("https://portfolio-stc.vercel.app/sendMail", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(n) }); if (o.ok) { const i = await o.json(); console.log("Réponse du serveur:", i), alert("Votre message a bien été envoyé. Merci " + n.n) } else { const i = await o.json(); console.error("Erreur du serveur:", i.message), alert("Erreur: " + i.message) } } catch (o) { console.error("Erreur de requête:", o), alert("Erreur lors de la communication avec le serveur.") } } else console.log("Email invalide "); e.target.closest("form").reset() }; s.addEventListener("submit", r) }; c(); u(); m(); d(); l(); a(); h();
